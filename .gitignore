# =========================
# OS / IDE
# =========================
.DS_Store
__pycache__/
*.pyc

# =========================
# Environment
# =========================
.env
*.env
.venv/
venv/
env/

# =========================
# Numpy / Embeddings
# =========================
*.npy
embedding/

!data/embeddings/**
!data/embedding_json/**
data_2
# =========================
# EDA / Intermediate Data
# =========================
eda/notebook/*.json
embedding_*.json
poi_merged.json
poi_with_id_region*.json
*_poi_id.json

# =========================
# Jupyter
# =========================
.ipynb_checkpoints/

*.json
*.jsonl
*.csv
*.log
*.txt

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they may contain local-only configuration
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files relevant for CI/CD environments
# !override_ci.tf

# Ignore CLI config files
.terraformrc
terraform.rc

# Terraform lock file
.terraform.lock.hcl

# Terraform state lock info (사용자님이 말씀하신 파일)
.terraform.tfstate.lock.info

# Python 관련
**/__pycache__/
*.py[cod]
.venv/
fastapi_app/uv.lock  # 팀 정책에 따라 다를 수 있지만 보통은 포함하지 않기도 함